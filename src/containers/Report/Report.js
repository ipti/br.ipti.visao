import React from "react";
import Loading from "../../components/Loading/CircularLoading";

const Home = props => {

  return (
    <>
      {false ? (
        <Loading />
      ) : (
        <>
          {/* <Reports  /> */}
        </>
      )}
    </>
  );
};


export default Home;
